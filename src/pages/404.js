import React from 'react'
import Template from 'templates/default'

export default class Index extends React.Component {
	render() {
		return (
			<Template>
				<h1>The page you have requested has either moved or no longer exists</h1>
			</Template>
		)
	}
}